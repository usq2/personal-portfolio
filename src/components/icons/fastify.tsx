import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function FastifyLogo({ height, width }: IconProps) {
  return <Icon icon="simple-icons:fastify" width={width} height={height} />;
}
