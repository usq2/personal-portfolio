import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function SQLLogo({ height, width }: IconProps) {
  return <Icon icon="grommet-icons:mysql" width={width} height={height} />;
}
