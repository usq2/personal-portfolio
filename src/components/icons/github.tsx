import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function GitLogo({ height, width }: IconProps) {
  return <Icon icon="icomoon-free:github" width={width} height={height} />;
}
