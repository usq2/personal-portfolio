import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function RedisLogo({ height, width }: IconProps) {
  return <Icon icon="devicon-plain:redis" width={width} height={height} />;
}
