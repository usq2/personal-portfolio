import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function OpenSearchLogo({ height, width }: IconProps) {
  return <Icon icon="simple-icons:opensearch" width={width} height={height} />;
}
