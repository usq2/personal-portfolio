import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function CppLogo({ height, width }: IconProps) {
  return <Icon icon="tabler:brand-cpp" width={width} height={height} />;
}
