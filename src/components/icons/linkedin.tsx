import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function LinkedInLogo({ height, width }: IconProps) {
  return <Icon icon="devicon-plain:linkedin" width={width} height={height} />;
}
