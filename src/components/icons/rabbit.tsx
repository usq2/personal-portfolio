import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function RabbitMQLogo({ height, width }: IconProps) {
  return <Icon icon="simple-icons:rabbitmq" width={width} height={height} />;
}
