import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function RESTApiLogo({ height, width }: IconProps) {
  return (
    <Icon
      icon="streamline-ultimate:coding-apps-website-web-dev-api-cloud"
      width={width}
      height={height}
    />
  );
}
