import { Icon } from "@iconify/react";
import type { IconProps } from "../../types/logo.types";

export default function ExpressJSLogo({ height, width }: IconProps) {
  return (
    <Icon icon="skill-icons:expressjs-light" width={width} height={height} />
  );
}
